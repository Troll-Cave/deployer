{
  "variables": [{
    "name": "user"
  }],
  "steps": [{
    "name": "run",
    "locals": [{
      "name": "bar"
    }],
    "type": "command",
    "actions": [{
      "command": "echo ${locals.bar}\ncat Dockerfile"
    }]
  }],
  "flow": [{
    "step": "run",
    "locals": {
      "bar": "${variables.user}"
    }
  }],
  "files": [{
    "name": "dockerfile",
    "location": "Dockerfile"
  }]
}